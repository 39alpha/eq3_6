all: $(TARGET)

FIXED_FLAGS := -std=f2008 -Wall -Wextra -Werror -pedantic -Wno-compare-reals -ffree-line-length-3000 -fmax-errors=0

$(TARGET): $(MODOBJECTS) $(OBJECTS) $(LIBRARIES)
	gfortran $(FIXED_FLAGS) $(FFLAGS) -Jmod -o $(TARGET) $^

obj/%.o: src/%.f90 $(MODOBJECTS)
	@mkdir -p mod obj
	gfortran $(FIXED_FLAGS) $(FFLAGS) -Jmod -c $(INCLUDES) -o $@ $<

../../lib/libeqlib.a:
	+make -C ../eqlib

../../lib/libeqlibu.a:
	+make -C ../eqlibu

../../lib/libeqlibg.a:
	+make -C ../eqlibg

clean:
	@rm -rf obj mod

.PHONY: clean
